in: query
name: expandAll
description: |
  Whether to return only basic properties or entire authority record.<br>
  Basic fields are:
    - id
    - tenantId
    - shared
    - headingType
    - authRefType
    - headingRef
    - sourceFileId
    - naturalId
    - numberOfTitles
schema:
  type: boolean
  default: false
