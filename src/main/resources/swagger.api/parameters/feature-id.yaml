name: featureId
in: path
required: true
description: Feature id (name)
schema:
  type: string
  enum:
    - search.all.fields
    - browse.contributors
    - browse.subjects
    - browse.classifications
    - browse.call-numbers
