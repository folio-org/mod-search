get:
  operationId: getQueryVersions
  summary: List query versions
  description: Returns query version availability for the current tenant
  tags:
    - index-management
  parameters:
    - $ref: '../../parameters/x-okapi-tenant-header.yaml'
  responses:
    '200':
      description: List of query versions
      content:
        application/json:
          schema:
            $ref: '../../schemas/entity/queryVersionCollection.yaml'
    '500':
      $ref: '../../responses/internalServerErrorResponse.yaml'
