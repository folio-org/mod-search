post:
  operationId: streamingReindexInstanceRecords
  summary: Streaming Re-Index
  description: Initiates streaming reindex by pulling records from mod-inventory-storage via HTTP
  tags:
    - index-management
  requestBody:
    required: false
    content:
      application/json:
        schema:
          $ref: '../../schemas/entity/indexSettings.yaml'
  parameters:
    - name: queryVersion
      in: query
      required: false
      schema:
        type: string
        default: '2'
      description: Query version to build the streaming reindex for
    - $ref: '../../parameters/x-okapi-tenant-header.yaml'
  responses:
    '202':
      description: Streaming reindex has been initiated
      content:
        application/json:
          schema:
            $ref: '../../schemas/entity/reindexFamilyJob.yaml'
    '400':
      $ref: '../../responses/badRequestResponse.yaml'
    '500':
      $ref: '../../responses/internalServerErrorResponse.yaml'
