post:
  operationId: submitIdsJob
  description: Creates a job for the stream of resource ids.
  tags:
    - search-resources-ids
  parameters:
    - $ref: '../../parameters/x-okapi-tenant-header.yaml'
  requestBody:
    content:
      application/json:
        example:
          $ref: '../../examples/streamIdsJobRequest.yaml'
        schema:
          $ref: '../../schemas/resourceIdsJob.yaml'
  responses:
    '200':
      description: 'Job for the stream of resource ids'
      content:
        application/json:
          example:
            $ref: '../../examples/streamIdsJobResponse.yaml'
          schema:
            $ref: '../../schemas/resourceIdsJob.yaml'
    '400':
      $ref: '../../responses/badRequestResponse.yaml'
    '500':
      $ref: '../../responses/internalServerErrorResponse.yaml'
