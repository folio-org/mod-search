type: object
properties:
  id:
    description: Option ID
    $ref: "browseOptionType.yaml"
  shelvingAlgorithm:
    description: Defines shelving order algorithm
    $ref: "shelvingOrderAlgorithmType.yaml"
  typeIds:
    description: Type IDs that should be used by the option
    type: array
    items:
      description: Type ID
      type: string
      format: uuid
required:
  - id
  - shelvingAlgorithm
